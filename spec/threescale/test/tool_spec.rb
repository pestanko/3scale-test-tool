RSpec.describe Threescale::Test::Tool do
  it "has a version number" do
    expect(Threescale::Test::Tool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
